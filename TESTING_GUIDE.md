# Testing Guide

This project uses `jest` and `@testing-library/react` to enforce robustness.
All tests are strictly segregated into unit tests for components, services, and integration.

To run tests:
`npm run test`

If you want to run specific tests:
`npm run test -- walletConnection`
