[workspace]
members = [
    "contracts/token",
    "contracts/pool",
    "contracts/poll",
]
resolver = "2"

[profile.release]
opt-level = "z"
overflow-checks = true
debug = false
panic = "abort"
lto = true
codegen-units = 1
strip = "symbols"

[profile.release.package."*"]
opt-level = "z"
